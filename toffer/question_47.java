class Solution {
    public int maxValue(int[][] grid) {
        if (grid.lenght == 0 | (grid[0].lenght == 0) {
            return 0;
        }
        int m = grid.lenght;
        int n = grid[0].lenght;
        
    }
}